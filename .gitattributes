# Handle line endings automatically for files detected as text
# and leave all files detected as binary untouched.
* text=auto

# Windows-specific files get CRLF line endings
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Linux-specific files get LF line endings
*.sh text eol=lf